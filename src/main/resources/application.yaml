ktor:
  application:
    modules:
      - ktor.ApplicationKt.module
  deployment:
    port: 8081
    host: "localhost"
  database:
    url: "jdbc:mariadb://localhost:3306/dbCountries"
    driver: "org.mariadb.jdbc.Driver"
    username: "josema"
    password: "josema"